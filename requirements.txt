# Windows only. The macOS build reaches Core Audio, AppKit, and Carbon through
# ctypes and draws its menu bar icons from SF Symbols, so it needs nothing
# beyond the Python standard library.
pycaw>=0.0.8; sys_platform == "win32"
pystray>=0.19.0; sys_platform == "win32"
Pillow>=9.0.0; sys_platform == "win32"
keyboard>=0.13.5; sys_platform == "win32"
pygame>=2.0.0; sys_platform == "win32"
comtypes>=1.1.0; sys_platform == "win32"
